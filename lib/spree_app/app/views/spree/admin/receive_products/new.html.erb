<%= render :partial => 'spree/admin/shared/purchase_order_tabs', :locals => {:current => 'Receive Product Details'} %>

<% if %w(purchased received).include?(@purchase_order.state) %>

    <h1><%= t(:receive_product) %> <%= @purchase_order.number %></h1>

    <%= form_for :receive_product, :url => admin_purchase_order_receive_products_path(@purchase_order), :method => :post do |f| %>
        <%= render :partial => 'form', :locals => {:f => f} %>

        <div data-hook="admin_purchase_order_new_form_buttons">
          <p class="form-buttons">
            <%= button t(:update) %>
            <%= t(:or) %> <%= link_to t(:back), admin_purchase_orders_url %>
          </p>
        </div>
    <% end %>
<% end %>